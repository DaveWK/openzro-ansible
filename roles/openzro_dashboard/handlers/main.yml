---
- name: reload systemd for the dashboard unit
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart openzro-dashboard
  ansible.builtin.systemd:
    name: "{{ openzro_dashboard_service }}"
    state: restarted
    daemon_reload: true
